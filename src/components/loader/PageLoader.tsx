import { LoaderText } from './LoaderText';

export const PageLoader = () => {
  return (
    <div className="flex h-body w-screen items-center justify-center">
      <LoaderText />
    </div>
  );
};
